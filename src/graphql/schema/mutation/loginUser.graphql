mutation Mutation($input: AuthBasicInput!) {
  authBasic(input: $input) {
    errors {
      message
      code
    }
    user {
      id
      name
      countryCode
      officeId
      picture
      phoneNumber
      role
      email
      isVerified
    }
    isSuccess
    successMessage
  }
}