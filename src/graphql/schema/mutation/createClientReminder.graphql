mutation CreateReminder($input: AddReminderInput!) {
  createReminder(input: $input) {
    reminder {
      id
      subject
      role
      dueDate
      createdBy
      clientId
      isCompleted
    }
    errors {
      message
      code
    }
  }
}
