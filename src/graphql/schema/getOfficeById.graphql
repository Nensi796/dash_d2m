query GetOfficeById($officeId: String) {
  getOfficeById(officeId: $officeId) {
    office {
      id
      name
      phoneNumber
      postCode
      address1
      address2
      address3
    }
    errors {
      message
      code
    }
  }
}