query Query($roles: [String]) {
  listUsers(roles: $roles) {
    nodes {
      id
      name
      countryCode
      officeId
      picture
      phoneNumber
      role
      email
      isVerified
    }
  }
}
